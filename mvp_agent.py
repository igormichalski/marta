"""OBSOLETO — não use.

Este era o protótipo inicial do fluxo multiagente. Foi substituído por
`framework.py`, que oferece uma CLI (`report`/`run`), lê a chave de API de
variável de ambiente (nunca do código) e faz a verificação de eficácia pela
contagem de mutantes sobreviventes.

Este arquivo pode ser apagado com segurança.
"""
